(ns image-server.game-manager-test
  (:require
   [cljs.test :as t :include-macros true]
   [image-server.game-manager :as sut]
   ))

(t/deftest test-handle-select
  (t/testing "handle-select"
    (t/is false)
    ))
